require 'spec_helper'

describe OptAlgFramework do
  it 'has a version number' do
    expect(OptAlgFramework::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
